<div class="span4" style="margin-top: 5px;">
  <div style="padding: 8px 0;" class="well">
    <ul class="nav nav-list">
      <li class="nav-header"><h4>รายงานผลกระทบ</h4>

      </li>
      <% if @effects %>
        <% @effects.each_with_index   do |effect,index|%>
          <li style="line-height: 2em;">
            <div style="float: left; width: 60%">
              <a href="/progresses/effect/<%= effect.id %>" class="menuheader"><%= index +1  %>).<%= effect.name_th %></a>
            </div>
            <div class="pull-right">
              <button class="btn btn-small btn-danger" type="button"> <%= effect.announces.size %>  </button>
            </div>
          </li>
        <% end %>
      <% end %>
      <div style="clear: both;"></div>

    </ul>
  </div>
</div><!--/span-->

<div class="span8" style="margin-top: 5px;">
  <p id="notice"><%= notice %>
  <div class="well">
    <h3 style="font-weight: bold; border-bottom: 2px solid #EEEEEE;"> <%= @announce.title %> </h3>
    <h5>
    ผลกระทบที่เกี่ยวข้อง :
    <% @announce.effects.each do |effect| %>
    <%= effect.name_th %> ,
  <% end %>
    </h5>

    <div class="small"> <%= @announce.created_at.strftime(" %m/%d/%Y  %I:%M%p") if @announce.created_at.strftime(":%m/%d/%Y") %>
   , จำนวนครั้งที่อ่าน:  <%= @announce.hit_count %> ครั้ง , <%= @announce.hits.last.user_ip if @announce.hits.last %>

    <br></div>
    <div class="small">
      <%= @announce.geopoint.name %>
      <%= @announce.geopoint.city%>
      <%= @announce.geopoint.country %> <br>
      [ ละติจูด: <%= @announce.geopoint.latitude %> , ลองติจูด:<%= @announce.geopoint.longitude %> ]
    </div>
    <div class="content-p well" style="margin-top: 20px;">
      <p><%=raw @announce.content %></p>
    </div>
    <div class="content-p" style="margin-top: 20px;">
      <h4>ตำแหน่งการรายงานผลกระทบ</h4>
      <% @json= @announce.geopoint.to_gmaps4rails %>
      <%= gmaps("markers" => {"data" => @json}, "map_options" => {"processing" => "json", "auto_zoom" => false, "zoom" => 15, "auto_adjust" => true }) %>


    </div>


  </div>


</div>
